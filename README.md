# Logistic Frontend Web (React + Vite)

```bash
cd frontend-web
npm install
npm run dev
```

Cấu hình API: sửa `src/api.js` nếu backend không chạy ở `http://localhost:4000`.
Tài khoản mẫu: `admin / admin123`.
